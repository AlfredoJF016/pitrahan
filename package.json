{
  "name": "pitrahan",
  "version": "1.0.0",
  "description": "piTrahan Bike Rental Platform — Yogyakarta",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "install-all": "npm install --prefix backend && npm install --prefix frontend"
  }
}
