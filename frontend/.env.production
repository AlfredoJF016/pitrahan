# Production environment variables for piTrahan Frontend
# This file is used when building for production (Vercel deployment)

# API Base URL — relative path because frontend and backend are on the same Vercel domain
VITE_API_BASE_URL=/api
